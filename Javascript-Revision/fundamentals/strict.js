// DIRECTIVE that looks like string. When placed at top of file makes sure that the file works the modern way
//This needs to be at top of the page

"use strict";

// MODER JS supports classes and modules - advanced language structures that enables use strict automatically. So we don't need to add the 'use strict' directive if we use them
